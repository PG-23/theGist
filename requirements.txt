# theGist — Python dependencies
# Install with: pip install -r requirements.txt
# Recommended: Python 3.11 in a conda environment

# Transcript ingestion
yt-dlp==2026.3.17

# Audio transcription (fallback when no captions available)
openai-whisper==20250625

# Local LLM inference
ollama==0.6.2

# Vector storage and semantic search
chromadb==1.5.9

# Sentence embeddings
sentence-transformers==5.5.1

# Deep learning backend (required by Whisper and sentence-transformers)
torch==2.12.0
torchvision==0.27.0

# Web interface
streamlit==1.58.0