[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "thegist"
version = "0.2.0"
requires-python = ">=3.11"

[project.scripts]
thegist = "thegist.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["thegist*"]